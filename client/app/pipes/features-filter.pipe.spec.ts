import {FeaturesFilterPipe} from './features-filter.pipe';

describe('FeaturesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FeaturesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
